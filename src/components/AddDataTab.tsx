import React from 'react';

import {Typography} from 'antd';

const {Title} = Typography;

export default function AddDataTab() {
  return (
    <div className="tab-add-data">
      <Title>Add Data</Title>
      Welcome to the <i>Add Data</i> tab!
    </div>
  );
}
